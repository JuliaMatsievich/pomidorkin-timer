import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  timerBtnContainer: {
    flexDirection: 'row',
    marginTop: 70,
    alignItems: 'center',
    gap: 30
  },
  timerBtnReset: {
    alignSelf: 'flex-end',
    marginRight: 30
  }
})
